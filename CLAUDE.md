# Mind — Session Rules

This repo is the user's personal mind-lab: mental training drills, notes, n-back research, side experiments. **Strict separation from utpap and work code.**

## Session-start protocol

1. `cd /Users/m4/Mind`
2. `git branch --show-current` — must say `main`. If not, `git checkout main`.
3. `git pull origin main`
4. Read the most recent file under `notes/` if any, so you know what was last on the user's mind.

## Hard rules

- Always work on `main`. No feature branches.
- Auto-commit + push on `main` after every change. Don't bypass hooks.
- Keep commits small and focused.
- Don't touch utpap, work code, or anything outside this repo from this session.

## Repo

- Location: `/Users/m4/Mind`
- Remote: `github.com/3KNG/Mind`
- Public (for GitHub Pages); flip private if site goes off-Pages.
