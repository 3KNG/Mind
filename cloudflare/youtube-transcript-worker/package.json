{
    "name": "youtube-transcript-worker",
    "version": "1.0.0",
    "private": true,
    "description": "Cloudflare Worker that returns YouTube transcripts via Browser Rendering.",
    "main": "src/worker.js",
    "type": "module",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "tail": "wrangler tail"
    },
    "dependencies": {
        "@cloudflare/puppeteer": "^0.0.14"
    },
    "devDependencies": {
        "wrangler": "^3.78.0"
    }
}
